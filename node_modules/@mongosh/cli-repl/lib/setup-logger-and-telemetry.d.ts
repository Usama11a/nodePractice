import type { Logger } from 'pino';
import type { MongoshBus } from '@mongosh/types';
interface MongoshAnalytics {
    identify(message: {
        userId: string;
        traits: {
            platform: string;
        };
    }): void;
    track(message: {
        userId: string;
        event: string;
        properties: {
            mongosh_version: string;
            [key: string]: any;
        };
    }): void;
}
export default function setupLoggerAndTelemetry(logId: string, bus: MongoshBus, makeLogger: () => Logger, makeAnalytics: () => MongoshAnalytics): void;
export {};
